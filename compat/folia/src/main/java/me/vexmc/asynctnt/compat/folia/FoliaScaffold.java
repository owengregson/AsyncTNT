package me.vexmc.asynctnt.compat.folia;

/**
 * Placeholder so the compat-folia source set is non-empty during scaffolding.
 * Replaced by {@code FoliaScheduling} / {@code FoliaRegionDispatch} in the
 * scheduling and engine tasks of the implementation plan.
 */
final class FoliaScaffold {
    private FoliaScaffold() {
    }
}
